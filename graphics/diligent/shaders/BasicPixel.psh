struct PSInput { 
    float4 Pos   : SV_POSITION;
    float2 UV    : TEXCOORD0;
};

struct PSOutput { 
    float4 Color : SV_TARGET; 
};

void main(in PSInput ps_input,
            out PSOutput ps_output)
{
    ps_output.Color = float4(ps_input.UV.x, ps_input.UV.y, 0.0, 1.0);
}